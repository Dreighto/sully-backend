# Sully Backend Consolidation Plan (2026-06-28)

> Generated by the consolidation-audit workflow. PLAN ONLY — every destructive step is gated.
> Status: awaiting operator JC-1/JC-2/JC-3 + go.

What happened: I merged the four Sully-backend audits (cruft purge, docs viability, dead-repo + reference sweep, structure blueprint) into ONE consolidation plan — target tree, keep/move/delete manifest, no-trace sweep, and a reversible migration order.

Does it work: This is a PLAN only. Nothing has been moved, deleted, or renamed. Every destructive step below has an explicit STOP-and-confirm gate that needs your go-ahead.

What you need to do: Read section 0 (the 3 judgment calls only you can settle) and approve/adjust. Once you say go, I execute in the sequenced order, pausing at each confirm gate.

---

# SULLY BACKEND CONSOLIDATION PLAN — for operator approval (2026-06-26)

Source audits: A1 (backend cruft), A2 (docs viability), A3 (dead-repo + reference sweep), A4 (structure blueprint). Live invariants that constrain everything below, confirmed across all four: `sully-backend.service` runs `node --env-file=.env build/index.js`, WorkingDirectory `~/dev/sully-backend`, listening `0.0.0.0:18779`; old `:18769` is dead. **Never touched at any step:** `data/companion.db` (+WAL/shm), `data/chat_uploads/`, `data/sully/`, `data/dispatch-prompts/`, `.env`, `build/`, and the service ExecStart.

---

## 0. THE 3 JUDGMENT CALLS — your decision before I start

**JC-1 — companion-speech: fold in or keep separate? → I recommend KEEP SEPARATE, rename to `sully-speech`.**
A4 makes the strong case: different hardware (Jetson scp-deploy to 10.10.10.2, no git/no internet), different runtime (two incompatible CUDA torch venvs vs Node/Vite), different failure domain (the STT bridge crash-looped 2742× silently once — that blast radius must not share CI with the chat API), and the seam is already a clean network contract (`ws://127.0.0.1:18770` STT, `http://127.0.0.1:18771` TTS; backend boots fine with speech off). Folding it in would *remove* a good boundary. **Action if you agree:** keep it a sibling repo, rename `companion-speech`→`sully-speech`, and add `sully-backend/docs/architecture/edge-speech-contract.md` pinning the WS/HTTP protocol + Jetson deploy note. **Open sub-question:** `companion-tts-venv` (7.7G) is referenced only by an *uninstalled* unit — A3 flags it as probably-orphaned but wants confirmation no on-demand GPU-TTS script spawns it before deletion. I will NOT delete it without that check + your gate.

**JC-2 — the heavy duplicate doc piles: delete or archive? → genuine A1/A2 conflict, your call.**
- `docs/reference-export/` (65MB per A1 / "31MB zip + unpacked" per A2): A1 says archive, **A2 says DELETE** ("Duplicate snapshots; trust the originals" per the master INDEX — biggest single cleanup win). The live SvelteKit source IS the source of truth, so this is a pure snapshot of dead code. **My recommendation: DELETE** (it's regenerable bloat, not provenance), but it's destructive so it gets a gate.
- `docs/Screenshots/` (17MB), `docs/agy-audit-shots/`, `docs/audit-shots/`, `flagship-visual-mockups/`: these are **AGY's artifacts** — per your standing rule "scope cleanup to own artifacts / don't delete others' files," I recommend **ARCHIVE, not delete**, even though they're heavy. **Your call:** if you want the space back, say so explicitly and I'll delete the AGY screenshot piles under a gate.

**JC-3 — iOS-app design docs + the canonical-docs-home tension: copy into backend or reference? → I recommend SPLIT BY OWNERSHIP.**
A4 wants a canonical `sully-backend/docs/architecture/`; A2 says `logueos-sully/docs` is already the well-organized canonical spine with the master INDEX.md. Resolution that honors both: **backend-architecture docs that describe the *backend* move INTO `sully-backend/docs/architecture/`** (backend-contract, service-map, edge-speech-contract, dispatch/routing, ecosystem-overview) because they describe the server, not the SwiftUI app. **SwiftUI build/design/scroll/token docs STAY in `logueos-sully/docs`** (frontend-owned) — backend only needs the *contract*, not the impl notes. `logueos-sully/docs/research/INDEX.md` (the master index, do-not-lose) becomes a **cross-repo index** pointing to both homes, with one-line stubs left where docs moved out. **If you'd rather keep ONE physical docs tree** (everything in logueos-sully/docs), say so — it's viable but weakens the "this repo is self-describing" goal.

---

## 1. TARGET STRUCTURE

```
~/dev/sully-backend/                  THE backend (Node/SvelteKit adapter-node, :18779)
├── src/
│   ├── routes/api/                   ← the product: 50+ live endpoints (chat, artifacts, surface, workspace, health)
│   ├── lib/server/                   ← the real backend: 60+ modules (chat_turn, llm_router, dispatch, voice_services, web_push, providers/, routing/)
│   ├── hooks.server.ts               ← required by adapter-node build
│   ├── app.html · app.d.ts           ← SvelteKit shell + ambient types (build needs them even API-only)
│   └── (frontend Svelte pages/components/PWA stripped — see §2, gated)
├── data/                             LIVE — never moves
│   ├── companion.db                  ← live operator DB
│   ├── chat_uploads/ · sully/ · dispatch-prompts/   ← live stores
│   └── _backups/                     ← NEW: stale *.bak-*.db + .pre-hygiene snapshots relocated here
├── docs/                             canonical home for BACKEND docs (JC-3)
│   ├── architecture/                 ← backend-contract, service-map, edge-speech-contract(NEW), dispatch-and-routing, ecosystem-overview
│   ├── backend/ · voice/ · research/ · design/ · backlog/   ← viable backend-relevant docs only
│   └── archive/                      ← quarantined SvelteKit/Capacitor-era history (NOT deleted)
├── scripts/ · tools/                 backend tooling only (training/STT-eval tooling relocated out — §3)
├── build/  .svelte-kit/  node_modules/   gitignored, regenerable, leave in place
├── .env · .env.example               live config (never moves)
├── package.json · svelte.config.js · vite.config.ts · tsconfig.json · eslint.config.js · .prettierrc
├── DEPLOY.md (NEW) · README.md (NEW)  ← "this is THE backend; one-repo deploy; sibling repos"
└── linux/ (archived old-name unit copies removed — live unit lives in /etc/systemd/system/)

STAYS SEPARATE (siblings under ~/dev/, coupled only by documented contracts):
  logueos-sully/   SwiftUI iOS app = the frontend. apple-node canonical for Swift; docs/ canonical for SwiftUI build+design.
                   Seam to backend = HTTP/SSE contract (parts[] UIMessage, provider field).
  sully-speech/    (was companion-speech) Python edge STT/TTS, room GPU + Jetson. Own deploy lifecycle.
                   Seam to backend = WS :18770 (STT) / HTTP :18771 (TTS), on-demand.
```

One-line rationale per top dir: **src/** = the only thing the running service compiles; **data/** = untouched live state; **docs/** = self-describing backend canon; **scripts/tools/** = backend maintenance only; **build/.svelte-kit/node_modules** = regenerable; **root configs** = build/lint tooling; **DEPLOY/README** = kill the "which repo is the backend" ambiguity.

---

## 2. BACKEND CRUFT PURGE

**Proof the live `:18779` build won't break (A1, confirmed via grep):** no API route (`+server.ts`) imports any frontend lib (`components/`, `motion/`, `native/`, `lib/chat/`, client `work-surface/`). So the SwiftUI-superseded SvelteKit frontend pages can be stripped and `vite build` still emits a valid API-only adapter-node server. The dependency cut is clean — but ANY src/ change is build-affecting and gets a rebuild+restart+verify gate.

**KEEP (the unified backend, no action):** `src/lib/server/**`, `src/routes/api/**`, `hooks.server.ts`, `app.html`, `app.d.ts`, `svelte.config.js`, `vite.config.ts`, `tsconfig.json`, `package.json`, `package-lock.json`, `eslint.config.js`, `.prettierrc`, `build/`, `.svelte-kit/`, `node_modules/`, and all live `data/` stores + `.env` + `.env.example` + `tools/send-test-push.mjs`.

**DELETE — regenerable/process cruft (safe, batched under one gate):** `unsloth_compiled_cache/` (leaked training cache), `.aider.chat.history.md`, `.aider.input.history`, `.aider.tags.cache.v4/`, `.superpowers/`, `logs/`, `test-results/`, `.e2e-data/`.
**[DESTRUCTIVE — SECURE-DELETE GATE]** `.env.bak-20260621-cc-callbackport` — contains provider secrets; superseded by live `.env`. `shred`/secure-delete, confirm first.

**ARCHIVE → `docs/archive/legacy-capacitor/` (Capacitor/iOS-shell era, retired by SwiftUI app; no server import):** `capacitor.config.ts`, `codemagic.yaml`, `assets/`, `static/ios/`, `scripts/ci-ios-icons.sh`, `scripts/ci-ios-patch.sh`, `scripts/gen-app-icons.sh`, `tools/trigger-ios-build.sh`, `linux/systemd/` (in-repo COPIES of old `logueos-companion.service` — the LIVE unit is `/etc/systemd/system/sully-backend.service`, untouched).

**ARCHIVE → `docs/archive/legacy-webui/` [BUILD-AFFECTING — rebuild+browser-QA GATE, fail-closed]:** the SvelteKit frontend pages + libs — `src/routes/chat/`, `settings/`, `avatar-preview/`, `work-surface-{dock-preview,flow-simulator,hybrid,preview}/`, `+layout.svelte`, `+layout.server.ts`, `+page.ts`; `src/lib/{components,motion,native,chat}/`; client-side `src/lib/work-surface/` (KEEP `src/lib/server/surfaceAdapter.ts` + `api/surface` — those are live backend); `src/service-worker.ts`, `src/app.css`, frontend `static/` PWA assets; `tests/e2e/`, `playwright.config.ts`, and UI-component vitest files (sully-button/card/pill, motion-spring, pill-anim, worker-pill-render, run-sheet-render). **Confirm-first checks before this gate clears:** (a) no PWA/Capacitor client still loads `/companion/chat` or bare root; (b) `static/pcm-capture-worklet.js` and `manifest`/icons aren't still served by a live API path. Recommend moving the UI half to a tagged branch + `docs/archive/legacy-webui/`, NOT `rm`.

**KEEP server tests, archive UI tests:** `tests/` has ~90 vitest files — MANY cover live backend logic (routing, dispatch, artifact-store, voice-services, decision-gate, verify-*). Split: keep server-logic tests, archive only the UI/visual ones with `tests/e2e/`.

**FOLLOW-UP after frontend strip (separate gated step, not blind):** prune dead deps from `package.json` — all `@capacitor/*` (8 pkgs), `@capacitor/assets`, `@playwright/test`, `@axe-core/playwright` — then `npm install` to regenerate `package-lock.json`, rebuild, restart, verify. Shrinks `node_modules/`.

**RELOCATE OUT of backend (training/STT-eval tooling — doesn't belong here):** `scripts/finetune/` + `extract_*_corpus.py` → a training/corpus repo; `tools/stt_ab/` → `sully-speech/tools/` (voice eval lives with speech). Gated as moves.

**UPDATE-NOW (code path, not cosmetic):** `src/routes/api/artifacts/_artifactService.ts:97` hardcodes fallback `/home/dreighto/dev/worktrees/LogueOS-Companion/w1` and the systemd unit does NOT set `LOGUEOS_ARTIFACT_WORKSPACE_DEFAULT`, so this dead-worktree path CAN be hit at runtime. Repoint to the unified backend root. (`src/lib/server/config.ts:45` is only a stale comment → SWEEP, §5.)

**INVESTIGATE before touching:** `data/escalation_corpus.jsonl` (untracked, Jun 27, shows modified — confirm the running backend isn't actively appending before any move); `.github/` workflows (strip retired Capacitor/Codemagic iOS-build CI, keep backend test/lint CI).

---

## 3. DOCS CONSOLIDATION

**Source-of-truth docs that MUST NOT be lost (do-not-lose, migrate BEFORE any source repo dies):**
1. `logueos-sully/docs/research/INDEX.md` — the canonical master index (updated 2026-06-22), maps AUTHORITATIVE vs SUPERSEDED across all four repos. Most load-bearing meta-doc; becomes the cross-repo index (JC-3).
2. `logueos-sully/docs/WORKER-GUIDE.md` — canonical task-by-task read list.
3. `sully-flutter-proof/docs/plans/swiftui-rebuild-blueprint.md` — INDEX-marked AUTHORITATIVE★ "THE rebuild plan" (79KB). **Flutter CODE is dead but this doc is live canon.** → `logueos-sully/docs/architecture/swiftui-rebuild-blueprint.md`.
4. `sully-flutter-proof/docs/research/swiftui-blueprint/gate1-7` — all AUTHORITATIVE; verified SwiftUI scroll/SSE/asset/typography research. → `logueos-sully/docs/research/swiftui-blueprint/`.
5. The CH-LOCKED design/voice canon in `logueos-sully/docs/{design,voice,skills,behavior,backend}` — meridian-oled-spec, chat-surface-component-spec, voice interaction-model, the 5 SKILL.md files, service-map. KEEP in place (frontend/design-owned).

**KEEP — migrate INTO `sully-backend/docs/` (uniquely live-referenced, backend-relevant; not duplicated in current tree):**
- `sully-backend/docs/2026-06-11-flagship-motion-layer.md` → `docs/research/flagship-motion-layer.md` (WORKER-GUIDE references for spring math).
- `docs/design/Sully_Work_Surface/sully_workflow_model.md` + `work_surface_api_contract.md` → `docs/design/work-surface/` (live-referenced workflow/dispatch model; the surrounding HTML/CSS mockups archive).
- Consolidate `docs/research/Sully_Eco.md` + `_v2` + `_reality-crossref` → ONE `docs/architecture/ecosystem-overview.md`; archive the superseded drafts (canonical v2 already in logueos-sully).
- The backend service-map + backend-contract (per JC-3) become canonical under `docs/architecture/` with stubs left in logueos-sully.

**ARCHIVE → `docs/archive/companion-svelte-era/` (SvelteKit/companion-era, superseded by SwiftUI pivot + rename; ~90% of the sully-backend/docs pile):** `blueprint.md`, `PLAN.md`, `DOING-NOW.md`, `CURRENT-STATE.md`, `SESSION-HANDOFF.md`, `OPERATOR-ACTIONS-2026-05-30.md`, the whole flagship-* dated cluster (visual-pass-plan, feel-research, hybrid-canon, polish-punchlist, agy-flagship-ux-teardown, motion-engine-fluidity-fix, anim-perf-measurement), `flagship-visual-mockups/`, `docs/design/` (sully-brand-brief + sully-locked-spec are explicitly SUPERSEDED by Meridian OLED spec — wrong fonts/colors; brand-concepts/, facelift-mockups/, ref_material/), `docs/audits/` + `2026-06-01-*-audit*` + `agy-mobile-audit-handoff.md`, `peer-reviews/` (root), `data/sully_behavior_audit_2026-06-04/`, `data/ch_data_chat.zip` (→ corpus archive). **AGY screenshot artifacts archive, not delete, per your own-artifacts rule** (`agy-audit-shots/`, `audit-shots/`, `Screenshots/`) — unless you override per JC-2.

**ARCHIVE → `docs/archive/backend-design/` (NUANCE — A2 catch):** `docs/superpowers/specs/` + `plans/` are mislabeled "irrelevant to SwiftUI" by the frontend-oriented INDEX, but they're the BACKEND architecture design-of-record (task-first-state-machine-v1, fact-gate, dispatcher, vision-multimodal, phase5-verified-workspace, artifact-promotion). They document LIVE backend behavior and may inform the hybrid-brain build. **Archive as backend history, do NOT delete.**

**DELETE — true duplicates/junk (gated, A2-confirmed):** `sully-backend/docs/research/workspace-context-comparison-2026-06.md` (byte-identical dup, md5 5b2c7b74… of the canonical copy); `logueos-sully/docs/this_is_fine.jpg` (905KB meme); and `docs/reference-export/` per JC-2. Archive the large raw build logs in `logueos-sully/docs/eval/` (690KB/296KB/63KB) but KEEP the .md phase reports.

**INVESTIGATE:** `docs/backlog/ios-voice-volume.md` — may be an unresolved SwiftUI-voice bug; verify against current voice canon → migrate to Linear if open, archive if fixed.

---

## 4. DEAD-REPO REMOVAL — with safety gates

**`~/dev/logueos-sully-backend` (373M) — DELETE-AFTER-SOAK.** Tagged `pro978-retired`, has `RETIRED.md`. Nothing live runs from it; the live unit points at `~/dev/sully-backend`. Its ONLY live role is the documented rollback path. **GATES:** (1) **fix the stale rollback line in `RETIRED.md` FIRST** — A4 found its `sed` rollback still targets `LogueOS-Companion/build` while the live unit already points at `sully-backend/build`, so the rollback is currently WRONG; fixing it makes rollback real. (2) Let the 24-48h soak on renamed `sully-backend.service` finish (RETIRED.md → ~2026-06-30). (3) Then `git archive` + delete under confirm gate. **Do NOT delete during the soak window — it's the only rollback artifact.** The `logueos-sully-backend-watchdog.timer/.service` are still active but run from the Orchestrator venv and monitor the *live* service — leave running; only the NAME is legacy (cosmetic rename, low priority).

**`~/dev/sully-flutter-proof` (732M) — DELETE after doc rescue + orphan-bak removal.** Dead Flutter PoC superseded by SwiftUI (`server/RELOCATED.md`). **GATES:** (1) migrate do-not-lose docs #3 + #4 from §3 FIRST. (2) Delete `/etc/systemd/system/sully-backend.service.pre-relocation.bak` — the one stale dangling reference (a `.bak`, not loaded by systemd, but it points at `sully-flutter-proof/server/build`). (3) Then the repo is fully unreferenced → delete under confirm gate.

**Orphan venvs:** `companion-speech-venv` (7.3G) is **LIVE** (active `logueos-companion-stt.service`) — **DO NOT DELETE.** `companion-tts-venv` (7.7G) is referenced only by an uninstalled unit — **probably orphaned, biggest single reclaim, but GATE:** confirm no on-demand GPU-TTS script spawns it before deletion (JC-1 sub-question).

---

## 5. NO-TRACE REFERENCE SWEEP — prioritized

**UPDATE-NOW (path-resolving — actively breaks/misroutes tooling, do FIRST, one Orchestrator PR):**
1. `tools/logueos_mcp_gateway/dispatch_tools.py:55` — `_APPROVED_TARGET_REPOS` frozenset still has `"LogueOS-Companion"` → dispatch to `sully-backend` is REJECTED until changed. **Highest priority.**
2. `gatekeeper/repo_resolution.py:47` + `gatekeeper/core.py:643` (user-facing approved-repos hint) — mirror #1.
3. `services/dispatch_listener/src/projects.js:28` — alias map key `'LogueOS-Companion'` → `sully-backend` (check downstream slug).
4. `tools/export_dispatch_corpus.py:58` — hardcoded `COMPANION_DB = .../LogueOS-Companion/data/companion.db` → **broken path right now**, reads nothing. Repoint to `sully-backend/data/companion.db`.
5. `tools/worker_smoke.py:9,21,87,626` — `DEFAULT_TARGET_REPO = "LogueOS-Companion"` → smoke harness targets a nonexistent dir, fails.
6. `tools/verify_merged_prs.py:44` + `tools/emit_cursor_run.py:16,50` — repo-map / example strings.
7. `src/routes/api/artifacts/_artifactService.ts:97` (§2 — the dead-worktree fallback).
8. **GitHub repo itself** is still `Dreighto/LogueOS-Companion` (`git remote -v`) — loudest live trace, path-resolving for `gh`/CI. Rename GH repo (or repoint `origin`).
- **Verify-before-clean:** `companion_tools.py:1258,1267` still have literal `"LogueOS-Companion"` strings — confirm they're a label/allow-list, not a second repo-name path check, before declaring clean. (`companion_tools.py:28` already = `sully-backend` ✓.)

**SWEEP (loaded at SESSION START — your "zero trace at session start" ask; first pass, exact files):**
1. `~/.claude/CLAUDE.md` (operator global) + `~/.claude/projects/.../memory/MEMORY.md` (the index — rename the companion/Sully-cluster hooks to sully-backend).
2. `~/dev/LogueOS-Orchestrator/.logueos/context/state-handoff-log.md` + `external-llm-briefing.md` + `current_lane.md`.
3. `.logueos/reference/{ports-and-services.md, linear-projects.md, endpoint_shape.md}`.
4. The ~49 `~/.claude/.../memory/*.md` body files — highest-traffic first: `project_companion_repos.md`, `reference_companion_backend_deploy_path.md`, `reference_sully_backend_live_db_and_uploads_paths.md`, `project_companion_backend_port_pro978.md`, `reference_companion_dispatch_plumbing.md`.
5. The ~15 `~/dev/logueos-sully/docs/*` design/handoff files carrying the old name.
6. Lower-priority code COMMENTS (cosmetic): `dispatch_listener/src/worktree.js:113`, `pool_utilization.js:6`, `config.ts:45`, plus archived `codemagic.yaml`/systemd copies/`.env.example` text — these mostly clear automatically once §2/§3 archive the files.

**LEAVE (append-only / historical — NEVER edit, per hard rule):** all `data/*.jsonl` (cc_completion_log + the 11 other ledgers), `data/workspaces/**`, `data/n8n_inbox/*`, `data/peer_reviews/2026-06-*` (PRO-978 gate evidence), `logueos-sully-backend/RETIRED.md` *narrative* (fix only its rollback line), git branch names `_parking_LogueOS-Companion-w*`, historical commit messages, `data/config/workspaces.json` + `worktree_*.json` (self-healing runtime state).

---

## 6. SEQUENCED MIGRATION — reversible, each step verifiable

Ordering principle: lowest-risk + dependency-unblocking first; highest-risk (frontend strip, repo deletes) last; nothing irreversible before its rescue/gate. **First thing I do = Step 1.**

**Step 1 — Make rollback real + unblock tooling (no risk to live service).** Fix `logueos-sully-backend/RETIRED.md` rollback path. Land the §5 UPDATE-NOW Orchestrator PR (#1-#7) — this immediately un-breaks `export_dispatch_corpus.py`, `worker_smoke.py`, and dispatch routing to `sully-backend`. *Verify:* dispatch a smoke job to `sully-backend` succeeds; `:18779` health unchanged. *Reversible:* git revert.

**Step 2 — Pure-delete cruft [GATE: regenerable-cruft confirm].** Remove `unsloth_compiled_cache/`, `.aider.*`, `.superpowers/`, `logs/`, `test-results/`, `.e2e-data/`. **[SEPARATE SECURE-DELETE GATE]** shred `.env.bak-*`. *Verify:* `npm run build` clean, service still green. *Reversible:* all gitignored/regenerable; secrets backup is intentionally non-recoverable.

**Step 3 — Tidy data/ [GATE: touches data/ dir].** Create `data/_backups/`, move `companion.bak-*.db` + `companion.db.pre-hygiene-*` + old snapshots into it; move `data/ch_data_chat.zip` + `sully_behavior_audit_2026-06-04/` to archive. **Never touch live `companion.db`/uploads/sully/dispatch-prompts.** *Verify:* `sqlite3 data/companion.db 'PRAGMA integrity_check'` + service health. *Reversible:* moves, not deletes.

**Step 4 — Quarantine docs (one commit, fully reversible).** Create `docs/archive/{companion-svelte-era,legacy-capacitor,backend-design}/`; move the §3 archive set + §2 Capacitor-era files in. *Verify:* `git mv` history intact; no live import referenced them. *Reversible:* git revert.

**Step 5 — Rescue do-not-lose docs, THEN consolidate.** Migrate §3 do-not-lose #3/#4 out of `sully-flutter-proof`; migrate the backend-relevant docs into `sully-backend/docs/architecture|design|research`; consolidate the 3 `Sully_Eco` → one; write `edge-speech-contract.md`, `DEPLOY.md`, `README.md`; update INDEX.md to the cross-repo form (JC-3); leave stubs where docs moved. **[GATE before any doc DELETE]** delete the byte-identical dup + meme + (per JC-2) `reference-export/`. *Verify:* every INDEX AUTHORITATIVE entry resolves to a live path. *Reversible up to the delete gate.*

**Step 6 — Session-start SWEEP.** Execute §5 SWEEP first pass (the exact files listed). *Verify:* fresh session start shows zero `LogueOS-Companion` in the loaded canon/memory/CLAUDE.md set; `grep -rl LogueOS-Companion` on the session-start file list returns empty. *Reversible:* git revert per repo.

**Step 7 — Rename siblings [GATE: service-unit changes].** `companion-speech`→`sully-speech` + unit renames `logueos-companion-stt/tts.service`→`sully-stt/tts.service` (coordinate `/etc/systemd/system/` reinstall + `daemon-reload`). GitHub repo rename + remote repoint. *Verify:* `:18770` STT still listening post-rename; backend chat round-trips. *Reversible:* rename back / repoint remote.

**Step 8 — Strip frontend from src/ [GATE: BUILD-AFFECTING, fail-closed, HIGHEST RISK — last].** Only after confirming no consumer of the Svelte PWA. Move UI half to a tagged branch + `docs/archive/legacy-webui/`, then prune `@capacitor/@playwright/@axe` deps, `npm install`, `npm run build`, restart, browser/API QA. *Verify:* `:18779` serves all live API routes; `companion_health` + a real chat turn pass. *Reversible:* tagged branch restores the UI; lockfile via git.

**Step 9 — Dead-repo deletes (post-soak, ~2026-06-30) [GATE each].** After soak passes: delete `logueos-sully-backend/`. After doc-rescue + `.bak` removal: delete `sully-flutter-proof/`. After JC-1 sub-check: delete `companion-tts-venv`. *Verify each:* service green, no broken unit references (`systemctl --failed` clean).

**End state:** clean tree, working `:18779`, zero `LogueOS-Companion` trace in the session-start canon/memory/CLAUDE.md set, three cleanly-separated sibling repos (sully-backend / logueos-sully / sully-speech), ~1.5G+ reclaimed (reference-export + flutter-proof + tts-venv pending your JC-2/JC-1 calls).

**I will not begin until you settle JC-1, JC-2, JC-3 and say go. Each [GATE] above is a hard stop where I show you exactly what's about to change and wait for confirm.**
