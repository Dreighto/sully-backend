node_modules/
/build
/.svelte-kit
.env
.env.*
data/*.db
data/*.db-wal
data/*.db-shm
data/chat_uploads/
# Personal data drops — chat exports, voice samples. Treat data/ as
# operator-only; only schema (in code) is committed, never the files.
data/*.zip
data/*.tar.gz
data/ch_data/

# QLoRA fine-tune scratch — venv + checkpoints live alongside the scripts
# but the artifacts themselves are heavyweight and never committed.
scripts/finetune/.venv/
scripts/finetune/__pycache__/
# Unsloth's per-run Triton kernel cache; lands in the cwd of train_qlora.py
# (= repo root by default). 3-10+ MB of compiled Python helpers per run.
unsloth_compiled_cache/

# Playwright e2e artifacts — traces, screenshots-on-failure, video, reports.
# Tests + config are committed; the run output is per-machine.
test-results/
playwright-report/
blob-report/
*.log

# Aider CLI session artifacts (operator's interactive tool)
.aider*
.superpowers/

# Capacitor — generated fresh in Codemagic CI, never committed
/ios
/www

# Per-spawn MCP config the dispatch listener writes into worker worktrees; if
# tracked it dirties the tree and dispatch fails with pre_spawn_dirty_refusal.
# Mirror LogueOS-Orchestrator / LogueOS-Console.
.mcp.json
mcp.json

# Transient dispatch prompt envelopes written at runtime
data/dispatch-prompts/
